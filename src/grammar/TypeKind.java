package grammar;

/** PP07 data type kind. */
public enum TypeKind {
	/** Integer base type. */
	INT,
	/** Boolean base type. */
	BOOL,
	/** Enum compound type. */
	ENUM,
	/** Void base type. */
	VOID,
	/** Char base type. */
	CHAR,
	/** Array compound type. */
	ARRAY;
}

