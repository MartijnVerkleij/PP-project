package grammar;

/** PP07 data types. */
public enum Type {
	/** Integer base type. */
	INT,
	/** Boolean base type. */
	BOOL,
	/** Enum compound type. */
	ENUM,
	/** Void base type. */
	VOID,
	/** Char base type. */
	CHAR,
	/** Array compound type. */
	ARRAY;
}

